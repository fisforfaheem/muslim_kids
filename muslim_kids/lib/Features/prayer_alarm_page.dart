import 'package:flutter/material.dart';

class PrayerAlarmPage extends StatelessWidget {
  const PrayerAlarmPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Prayer Alarm'),
      ),
      body: Center(
        child: Text('Prayer Alarm Page'),
      ),
    );
  }
}
